# Production Environment Variables Template
# Copy these to Vercel Dashboard → Your Project → Settings → Environment Variables

# ============================================
# CLIENT ENVIRONMENT VARIABLES (Frontend)
# ============================================

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# API Configuration
# IMPORTANT: Update this after first deployment with your actual Vercel URL
VITE_API_URL=https://your-project-name.vercel.app/api

# Pexels API (for image service)
VITE_PEXELS_API_KEY=your_pexels_api_key_here


# ============================================
# SERVER ENVIRONMENT VARIABLES (Backend)
# ============================================

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here

# JWT Configuration
# Generate a secure random string (at least 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_secure_random_jwt_secret_here

# Server Configuration
PORT=3001
NODE_ENV=production

# Frontend URL
# IMPORTANT: Update this with your actual Vercel deployment URL
FRONTEND_URL=https://your-project-name.vercel.app

# Admin Configuration
# Comma-separated list of admin email addresses
ADMIN_EMAILS=admin@example.com,another-admin@example.com


# ============================================
# NOTES
# ============================================

# 1. Never commit actual .env files to Git
# 2. All variables prefixed with VITE_ are exposed to the client
# 3. Server variables are kept secure on the backend
# 4. Update VITE_API_URL and FRONTEND_URL after first deployment
# 5. Generate a strong JWT_SECRET for production
# 6. Add your actual admin emails to ADMIN_EMAILS
